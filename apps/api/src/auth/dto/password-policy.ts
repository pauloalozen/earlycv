export const USER_PASSWORD_POLICY_REGEX = /^(?=.*[A-Z])(?=.*\d).{8,}$/;

export const USER_PASSWORD_POLICY_MESSAGE =
  "password must have at least 8 characters, one uppercase letter, and one number";
